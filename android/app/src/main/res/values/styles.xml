<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
    </style>

    <declare-styleable name="ReactMaterialView">
        <attr name="materialTitle" format="string"/>
        <attr name="materialColor" format="color"/>
        <attr name="materialSize" format="dimension"/>
    </declare-styleable>
</resources>
